version: '3'
services:
  app:
    build: .
    container_name: apisiteperso-app-1
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
    environment:
      NODE_ENV: production
    env_file:
      - .env